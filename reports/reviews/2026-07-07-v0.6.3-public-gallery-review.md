# Release Readiness Review - portfolio-risk-compass v0.6.3

Date: 2026-07-07 CST
Increment: public gallery landing page
Decision: PASS for local release checkpoint; no workflow or push changes made.

## Scope

- Added a no-JavaScript `gallery.html` public landing page for the checked-in demo output bundle.
- Linked the dashboard, demo manifest, walkthrough, visual release checklist, release manifest, docs export, regeneration commands, verification commands, and safety boundaries from one local static route.
- Added `gallery.html` to demo bundle generation, visual capture audit coverage, visual release checklist readiness checks, selfcheck required artifacts, README output documentation, generated docs export, and release manifest inventory.
- Bumped package metadata to `0.6.3`.

## Public Hygiene

- Gallery links are restricted to local static targets; absolute URLs, parent traversal, backslashes, and control characters are rejected.
- The gallery records checked-in demo fixture outputs only and does not expose private account data, private paths, secrets, broker connectivity, live market data, order entry, position sizing, recommendations, or investment advice.
- Screenshot capture remains optional release-owner evidence and is not required for this checkpoint.

## Verification

- `PYTHONPATH=src python -m unittest discover -s tests` passed.
- `python scripts/privacy_scan.py` passed.
- `python scripts/selfcheck.py` passed, including deterministic demo regeneration, public gallery generation, docs export, release manifest, privacy scan, and package audit.
- `git diff --check` passed.

## Follow-up

- Capture `screenshots/dashboard-public-review-1365x900.png` only when release-owner screenshot evidence is required.
